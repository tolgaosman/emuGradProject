{
  "name": "emugradproject",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "api": "backend\\venv\\Scripts\\python.exe backend/app.py",
    "build": "npm --prefix frontend run build && node -e \"require('fs').cpSync('frontend/dist', '.', {recursive: true})\"",
    "lint": "npm --prefix frontend run lint"
  }
}
